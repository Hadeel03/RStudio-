print("hello woRld") 
print(4)
my_apples <- 4 # Object assignment operator... shortcut on Mac (Option + -)
my_apples - 1 # R language is case sensitive
n_apples <- c(7, 10, 3)
n_apples <- c(7, my_apples, 3)
names <- c("Menglin", "Gloria", "Robert") # Whatever in quotes is not defined in R
names
data.frame(person = names, apple_count = n_apples)
friends <- data.frame(person = names, apple_count = n_apples)

# if you need help use ? followed by function or object name
